import React from 'react'
import UsersContainer from './'

describe('UsersContainer container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
